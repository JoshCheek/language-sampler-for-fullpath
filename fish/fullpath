#!/usr/bin/env fish

set paths $argv

if test (count $paths) -eq 0
  while read -l line
    set paths $paths $line
  end
end

if test (count $paths) -eq 1
  echo -n $PWD/$paths[1]
else
  for arg in $paths
    echo $PWD/$arg
  end
end
